import React, { PureComponent } from 'react';
import Main from '../common/components/main';

export default class DoubleMicro extends PureComponent {
    render() {
        return <Main moduleType={23} editModuleType={30} />;
    }
}
